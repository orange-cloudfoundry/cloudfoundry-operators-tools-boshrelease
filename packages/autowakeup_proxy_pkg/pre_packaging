set -e
set -u
set -x

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/autosleep/spring-apps/autowakeup-proxy
../../gradlew build
#./mvnw clean install -DskipTests=true --settings settings.xml
mv build/bin/autowakeup-proxy-0.3.0-SNAPSHOT.war  ${BUILD_DIR}


cd ${BUILD_DIR}
rm -rf autosleep

