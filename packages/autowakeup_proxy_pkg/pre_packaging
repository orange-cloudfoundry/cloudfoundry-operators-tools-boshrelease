set -e
set -u
set -x

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/autosleep/spring-apps/autowakeup-proxy
../../gradlew build
mv build/libs/org.cloudfoundry.autosleep.autowakeup-proxy-0.3.0.war ${BUILD_DIR}


cd ${BUILD_DIR}
rm -rf autosleep

