set -e
set -u
set -x

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/autosleep/springapps/
./mvnw clean install -DskipTests=true --settings settings.xml
mv target/bosh-deployer-0.0.1-SNAPSHOT.jar  ${BUILD_DIR}


cd ${BUILD_DIR}
rm -rf bosh-deployer

