set -e
set -u
set -x

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/autosleep/spring-apps/autosleep-core

../../gradlew build
#./mvnw clean install -DskipTests=true --settings settings.xml
mv build/bin/autosleep-core-0.3.0-SNAPSHOT.war  ${BUILD_DIR}


cd ${BUILD_DIR}
rm -rf autosleep

